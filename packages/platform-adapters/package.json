{
  "name": "@cola-finance/platform-adapters",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/mock-data && cp src/mock-data/*.csv dist/mock-data/",
    "lint": "echo \"skip lint\"",
    "test": "echo \"no tests yet\""
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "eslint": "^9.0.0",
    "typescript": "^5.9.0"
  }
}
