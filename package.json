{
  "name": "cola-finance",
  "private": true,
  "version": "0.1.0",
  "description": "Cola Finance monorepo",
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.24.0"
}
